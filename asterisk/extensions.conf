
[general]

[global]

[internal]

exten => 01,1,Answer()
   same => n,Background(basic-pbx-ivr-main)
   same => n,WaitExten(8)
   same => n,Background(vm-repeat)
   same => n,WaitExten(5)
   same => n,Playback(goodbye)  
   same => n,Hangup()

      exten => 1,1,Playback(digits/1)
         same => n,Goto(sales department,1,2) 

      exten => 2,1,Playback(digits/2)
         same => n,Goto(technical support,1,2)

      exten => 3,1,Playback(digits/3)
         same => n,Goto(finance department,1,2)

      exten => 4,1,Playback(digits/4)
         same => n,Goto(employee catologist,1,2)   

      exten => 5,1,Playback(digits/5)
         same => n,Goto(internal,01,2)

      exten => 0,1,Playback(digits/0)
         same => n,Goto(operator,1,2)   

      exten => 91,1,Playback(digits/91)
         same => n,Goto(oper1,1,2)   

      exten => 92,1,Playback(digits/92)
         same => n,Goto(oper2,1,2)     

[sales department]   
exten => 1,1,Answer()
   same => n,Background(pm-invalid-option&vm-starmain)
   same => n,WaitExten(5)
   same => n,Playback(goodbye)  
   same => n,Hangup()

exten => *,1,Playback(digits/*)
   same => n,Goto(internal,01,2)

[technical support]
exten => 1,1,Answer()
   same => n,Background(pm-invalid-option&vm-starmain)
   same => n,WaitExten(5)
   same => n,Playback(goodbye)  
   same => n,Hangup()

exten => *,1,Playback(digits/*)
   same => n,Goto(internal,01,2)

[finance department]
exten => 1,1,Answer()
   same => n,Background(pm-invalid-option&vm-starmain)
   same => n,WaitExten(5)
   same => n,Playback(goodbye)  
   same => n,Hangup()

exten => *,1,Playback(digits/*)
   same => n,Goto(internal,01,2)

[employee catologist]
exten => 1,1,Answer()
   same => n,Background(pm-invalid-option&vm-starmain)
   same => n,WaitExten(5)
   same => n,Playback(goodbye)  
   same => n,Hangup()

exten => *,1,Playback(digits/*)
   same => n,Goto(internal,01,2)

[operator]
exten => 1,1,Answer() 
   same => n,Playback(conf-now-recording)
   ;same => n,Set(fname=${STRFTIME(${EPOCH},,%Y-%m-%d-%H-%M%S)}-${CALLERID(num)}-${UNIQUEID})
   ;same => n,Set(MONITOR_FILENAME=record${fname}.wav,br(record-in${fname}.wav),t(record-out${fname}.wav))
   ;same => n,MixMonitor(record/${fname}.wav,br(record-in/${fname}.wav),t(record-out/${fname}.wav))
   same => n,Queue(operator)
   ;same => n,Queue(operator,M(recordcalls))
   same => n,Hangup() 

[macro-recordcalls]
; Запись:
; - ${UNIQUEID}-mixed.wav (общий микс)
; - ${UNIQUEID}-in.wav (только звонящий) 
; - ${UNIQUEID}-out.wav (только отвечающий)
exten => s,1,NoOp(Start Monitor)
   same => n,Set(fname=${STRFTIME(${EPOCH},,%Y-%m-%d-%H-%M%S)}-${CALLERID(num)}-${CONNECTEDLINE(num)})
   same => n,MixMonitor(record/${fname}.wav,br(record-in/${fname}.wav),t(record-out/${fname}.wav))

;exten => s,1,MixMonitor(
;    /var/spool/asterisk/monitor/${UNIQUEID}-mixed.wav,
;    b(/var/spool/asterisk/monitor/${UNIQUEID}-in.wav)
;    u(/var/spool/asterisk/monitor/${UNIQUEID}-out.wav)
;)

;exten => 1,1,NoOp(${CALLERID(num)})
;   same => n,Set(AUDIOHOOK_INHERIT(MixMonitor)=yes)
;   same => n,Set(fname=${CDR(uniqueid)}.g729)
;   same => n,Set(recdir_temporary=/var/spool/asterisk/monitor/inbound_records/${STRFTIME(,,${MACRO_CONTEXT})})
;   same => n,MixMonitor(${recdir_temporary}/${fname})
;   same => n,Set(fix_clid=${CALLERID(num)})
;   same => n,Queue(operator)
;   same => 1,System(mkdir -p ${recdir_temporary}/${DIALEDPEERNUMBER})
;   same => n,Set(recdir_final=${recdir_temporary}/${DIALEDPEERNUMBER})
;   same => n,Set(fname2=${recdir_final}/${STRFTIME(${EPOCH},,%d_%m_%Y_%Hh_%Mmin_%Ssec)}_from_${fix_clid}_to_${DIALEDPEERNUMBER}.g729)
;   same => n,System(mv ${recdir_temporary}/${fname} ${fname2})

[oper1]
exten => 101,1,Answer()
   same => n,Playback(conf-now-recording)
   same => n,MixMonitor(record.wav,br(record-in.wav)t(record-out.wav))
   same => n,Dial(PJSIP/oper1,5)
   same => n,Playback(telephone-number&vm-isunavail)
   same => n,Hangup()  

[oper2]
exten => 102,1,Answer()
   same => n,Playback(conf-now-recording)
   same => n,MixMonitor(record.wav,br(record-in.wav)t(record-out.wav))
   same => n,Dial(PJSIP/oper2,5)
   same => n,Playback(telephone-number&vm-isunavail)
   same => n,Hangup() 

#exten => _userX,1,Answer()
#   same => n,Dial(PJSIP/${EXTEN},5)
#   same => n,Playback(telephone-number&vm-isunavail)
#   same => n,Hangup()

#exten => hello,1,Answer()
#   same => n,Playback(hello-world)
#   same => n,Hangup()

#exten => echo,1,Answer()
#   same => n,MixMonitor(record.wav,br(record-in.wav)t(record-out.wav))
#   same => n,Echo()
#   same => n,Hangup()

#exten => 500,1,Playback(vm-intro)
#   same => n,Record(daygreeting.wav)
#   same => n,Wait(2)
#   same => n,Playback(daygreeting)
#   same => n,Hangup   